"""
Filename Normalizer Module

⚠️  이 패키지의 정규화 로직 파일들은 _cleanup_temp로 이동됐습니다.
    (rename_normalize.py, rename_normalize_gui.py)

✅  GUI가 실제 사용하는 정본 파일:
    modules/organizer/renameFiles/rename_normalize.py
    modules/organizer/renameFiles/rename_normalize_gui.py

정규화 함수 import 방법:
    from modules.organizer.renameFiles.rename_normalize import (
        normalize_line,
        normalize_line_without_genre_inference,
        infer_genre_from_filename,
    )
"""
