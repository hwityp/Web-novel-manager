# Changelog

이 프로젝트의 모든 주요 변경 사항을 기록합니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 따르며,
버전 관리는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [v1.3.36] - 2026-09-07

### Fixed

- **CJK 전각 괄호(`（`, `）`, `【`, `】`, `［`, `］`) 전면 지원 (`core/title_anchor_extractor.py`, `core/utils/novel_trait_extractor.py`):**
  - 원문 한자 제목 파싱(`parse_foreign_title_info`), 판본 태그(`EDITION_TAG_PATTERNS`), 완결 마커(`COMPLETION_PATTERNS`, `paren_completion_match`), 접두사 태그(`prefix_bracket`), 첨언 추출(`NovelTraitExtractor.extract_from_annotations`) 전반에 전각 괄호 및 브래킷 지원 추가.
  - 전각 괄호로 둘러싸인 중국 원문 제목이 반각 괄호 누락으로 인해 정규식 폴백에 의해 잘리거나 분해(예: `( 54年，邻居傻柱) (四合院：重生)`)되는 현상을 원천 차단.
  - 소괄호 내 2글자 이상의 CJK 원문 제목은 장르/특성 첨언 추출 대상에서 제외하도록 안전장치 마련.
  - 결과: `사합원 중생54년, 인거사주（四合院：重生54年，邻居傻柱） 1-668 완.txt` $\rightarrow$ `사합원 중생54년, 인거사주(四合院：重生54年，邻居傻柱) 1-668 (완).txt` 및 `절세신기（绝世神器） (19N) 1-1053 완.txt` $\rightarrow$ `절세신기(绝世神器) (19N) 1-1053 (완).txt` 정상 정규화.
- **장르 키워드 오분류 방지 (`modules/classifier/genre_keywords.json`, `modules/classifier/src/data/genre_keywords.json`):**
  - 역사 장르 키워드에서 `"사합원": 10` 제거. 시대적 배경 용어가 웹소설 제목에 포함되었다고 해서 검색 미분류 작품이 `[역사, 사합원]`으로 잘못 강제 분류되는 오류 방지.
- **네이버 검색 스니펫 오탐지 방지 및 제목 검증 강화 (`modules/classifier/src/core/naver_genre_extractor_v4.py`):**
  - 검색 결과 파싱 시 거대 웹페이지 래퍼(네이버 웨일 다운로드, 외부 광고 등 800자 초과 블록)가 스니펫으로 수집되어 무관한 `현판` 배지가 오인식되는 문제 해결 (컨테이너 길이 `15 < len <= 800` 제한).
  - 스니펫 텍스트 내 대상 소설 제목(2글자 이상) 존재 여부 검증 및 유효 플랫폼 URL/키워드 검증을 거친 경우에만 장르를 추출하도록 보강.
  - 결과: `절세신기` 검색 시 불일치 작품으로부터 `[현판]`이 잘못 주입되는 오분류 방지.

## [v1.3.35] - 2026-09-07

### Fixed

- **패러디 팬덤 해시태그 보존 개선 (`core/utils/novel_trait_extractor.py`, `core/title_anchor_extractor.py`):**
  - `PARODY_FANDOM_MAP` 및 `TRAIT_PATTERNS`에 `마블`(어벤져스, 아이언맨, marvel), `포켓몬스터`, `DC`(배트맨, 슈퍼맨) 추가.
  - 패러디 컨텍스트에서 첨언 해시태그(`hashtags`)의 토큰이 누락되지 않고 특징 키워드로 안전하게 보존되도록 개선.
  - 결과: `#패러디 #마블` $\rightarrow$ `[패러디, 마블]`, `#패러디 #포켓몬스터` $\rightarrow$ `[패러디, 포켓몬스터]` 정상 정규화.
- **CJK 원문 괄호 내 숫자/문장부호 허용 및 공백 보존 (`core/title_anchor_extractor.py`, `core/adapters/filename_normalizer_adapter.py`):**
  - `parse_foreign_title_info`의 괄호 CJK 매칭식을 개선하여 내부에 아라비아 숫자(예: `54年`) 및 문장부호(`：`, `，`)가 포함되어도 원문 전체가 누락 없이 올바르게 추출되도록 수정.
  - 원본 제목과 CJK 괄호 사이의 공백 여부(`has_space_before_foreign`)를 파싱 및 보존하여, 원본에 공백이 없는 경우 인위적인 공백 추가 없이 자연스럽게 밀착 유지.
  - 결과: `인거사주(四合院：重生54年，邻居傻柱)` $\rightarrow$ `인거사주(四合院：重生54年，邻居傻柱)` 정상 정규화.
- **성인 등급/에디션 태그 `(19N)` 보존 및 위치 순서 수정 (`core/title_anchor_extractor.py`):**
  - `EDITION_TAG_PATTERNS`에 `19[Nn]` 및 `19禁` 추가.
  - `(19N)`이 제목 내부로 혼입되지 않고 에디션/판본 정보(`edition_info`)로 분리되어 제목과 원문 CJK 제목 뒤, 회차 범위 앞에 올바른 순서로 배치되도록 수정.
  - 결과: `절세신기(绝世神器) (19N) 1-1053 완.txt` $\rightarrow$ `절세신기(绝世神器) (19N) 1-1053 (완).txt` 정상 정규화.

### Changed

- **GUI 결과 목록(Treeview) 장르 컬럼 너비 확장 (`gui/main_window.py`):**
  - 메인 결과 테이블의 "장르" 컬럼 기본 및 최소 너비를 기존 대비 3배인 360px로 확대하여(`width=360`, `minwidth=360`), 긴 복합 장르 및 태그 텍스트의 가독성 및 시인성을 개선.

## [v1.3.34] - 2026-09-07

### Fixed

- **다중 단어 해시태그 첨언 및 코난 패러디 인식 개선 (`core/utils/novel_trait_extractor.py`, `core/title_anchor_extractor.py`):**
  - 해시태그 파싱 정규식을 개선하여 `#명탐정 코난`, `#귀멸의 칼날` 등 공백이 포함된 다중 단어 해시태그를 온전히 추출하도록 지원 (수치 범위 및 완결 마커 오인식 방지 룩어헤드 적용).
  - 대표 패러디 매핑(`PARODY_FANDOM_MAP`) 및 특징 패턴(`TRAIT_PATTERNS`)에 `명탐정 코난`(`코난`, `명탐정코난`, `남도일`, `검은 조직` 등), `주술회전`, `귀멸의 칼날` 추가.
  - 첨언 토큰화 시 쉼표/슬래시 분리 후 다중 단어 키워드가 직접 일치하는 경우 단일 토큰으로 보존하여 장르/특성이 유실되지 않도록 개선.
  - 제목 노이즈 제거 시 다중 단어 해시태그 전체를 깨끗하게 제거하여 파일명 잔여 텍스트(`residual`)에 `코난` 등이 불필요하게 잔류하지 않도록 수정.
  - 적용 결과: `가남：개국절호명미，와저주창 1-740 完 (AI번역) #패러디 #명탐정 코난.txt` $\rightarrow$ `[패러디, 명탐정 코난] 가남：개국절호명미，와저주창 1-740 (완).txt` 정상 정규화.

## [v1.3.33] - 2026-09-07

### Fixed

- **파일명 정규화 오류 2건 수정 (`core/title_anchor_extractor.py`):**
  - 후기 마커 완결 판정: `후기`, `에필`, `에필로그`, `후일담` 등 완결성 마커 감지 시 `is_completed = True`로 판정하여 `(완)` 자동 부여 (`나는 엑스트라를 원한다 1-320 + 후기.txt` $\rightarrow$ `나는 엑스트라를 원한다 1-320 (완) + 후기.txt`).
  - 단일 숫자 + 본편 및 외전 분리: 단위 패턴 및 숫자 lookahead에 `본편`을 추가하고 복합 패턴 처리 개선 (`삼국지 유현덕의 천재아들 165본편 및 외전 (완).txt` $\rightarrow$ `삼국지 유현덕의 천재아들 1-165 (완) + 외전.txt`).
- **파일명 정규화 선행 시 첨언 우선 장르 추론 보존 (`core/adapters/filename_normalizer_adapter.py`, `core/adapters/folder_organizer_adapter.py`, `core/adapters/genre_classifier_adapter.py`):**
  - 파일명 정규화(Stage 1.5: `parse_only`) 시점에 `NovelTraitExtractor.extract_from_annotations()`를 선행 실행하여 첨언 장르(`task.genre`)를 즉시 확정 및 캐시에 보존.
  - 정규화 미리보기(`preview_normalized_name`) 생성 시 확정된 장르 태그(`[장르]`)가 파일명에 바로 포함되도록 보장.
  - `NovelTask` 생성 시점부터 `metadata['original_raw_name']`을 영구 보존하여 소스 폴더에 정규화된 파일명이 즉시 저장(rename)되더라도 이후 Stage 2 장르 추론에서 원본 첨언을 잃어버리지 않도록 개선.

## [v1.3.32] - 2026-09-07

### Added

- **파일명 첨언(Annotation) 우선 장르/특성 추론 엔진 신설 (`core/utils/novel_trait_extractor.py`, `core/adapters/genre_classifier_adapter.py`):**
  - 파일명의 접두사 대괄호 태그(`[...]`), 첨언 소괄호(`(...)`), 해시태그(`#...`)로부터 장르 및 세부 특성을 최우선으로 자동 파싱하는 `NovelTraitExtractor.extract_from_annotations()` 신설.
  - 첨언에서 유효한 장르가 발견되면 외부 웹 검색 API(네이버/구글) 호출을 완전히 건너뛰고 즉시 확정(`confidence='high'`, `source='annotation'`)하여 네트워크 할당량 소진 방지 및 처리 속도 대폭 향상.
  - 복합 태그 분해 지원: `[나루토패러디 시스템 AI번역]` $\rightarrow$ 주 장르: `패러디`, 특성: `나루토`, `시스템` $\rightarrow$ `[패러디, 나루토, 시스템]` 자동 정규화.
  - 세부 특성 자동 정규화 매핑: `연대` $\rightarrow$ `연대물`, `해리포터`/`나루토` $\rightarrow$ 팬덤 특성, `시스템`, `사합원`, `궁투` 등.
  - `AI번역`, `기계번역`, `손번역`, `txt`, `텍본` 등 번역 및 플랫폼 노이즈 자동 필터링.
- **'패러디' 장르 표준 화이트리스트 및 매핑 규칙 교정 (`config/genre_mapping.json`):**
  - 기존에 `패러디` 장르가 `판타지`로 잘못 다운그레이드 매핑되던 오류를 바로잡아, 표준 승인 장르인 `패러디`로 온전히 보존 및 화이트리스트 등록.

### Fixed

- **복합 완결+외전(`完外`) 표기 파일명 정규화 누락 오류 수정 (`core/title_anchor_extractor.py`):**
  - `parse_foreign_title_info`에서 `完外`, `番外`, `全` 등 마커 한자 조합을 외국어 원제(original foreign title)로 오인식하여 제목 정리 시 삭제되던 문제를 `CJK_MARKER_CHARS` 검증 도입으로 원천 차단.
  - `COMPLETION_PATTERNS` 및 `_parse_residual`에 `(?:完|완)[\s,]*\+?[\s,]*(?:外|외(?:전|포)?)` 복합 정규식을 적용하여 `궁투불여양조구 完外.txt` $\rightarrow$ `궁투불여양조구 (완) + 외전.txt`로 완결과 외전 정보가 동시에 완벽히 정규화되도록 조치.
  - 해시태그(`#패러디`, `#해리포터` 등) 및 선행 메타 태그 브래킷을 순수 제목에서 깔끔히 제거하여 제목 훼손 방지.
- **단위 및 회귀 테스트 스위트 확장 (`tests/test_annotation_and_normalization.py`):**
  - 사용자 요구 5대 대표 예시 및 파이프라인 통합 정규화 검증을 포함한 6개 자동화 테스트 신설 (전체 122개 테스트 100% 통과 유지).

## [v1.3.31] - 2026-08-02

### Added

- **중국 로판/로맨스 소설의 '언정' 장르 자동 매핑 및 태그 정규화 (`core/utils/genre_mapping.py`, `core/utils/novel_trait_extractor.py`):**
  - 제목 및 파일명에서 한자(`[\u4e00-\u9fff\u3400-\u4dbf]`) 또는 한자음/중목 로맨스 키워드(`천월`, `비빈`, `낭낭`, `계후`, `소교낭`, `약향농`, `복운`, `육령`, `소저`, `공자`, `악독`, `미인`, `종전`, `매매`, `사합원`, `부인`, `수모`, `단총`, `개가`, `계모`, `교처`, `군관`, `미색`, `극본` 등)를 감지하는 `is_chinese_romance` 판별 로직 신설.
  - 추출된 표준 장르가 `로판` 또는 `로맨스`인 중국 웹소설을 표준 화이트리스트 장르인 **`언정`**으로 자동 전환하고, 태그를 `[언정, 빙의]` 형태로 정규화하도록 개선.
- **웹소설 서브장르 및 중국 소설 키워드 사전 확장 (`genre_keywords.json` v1.5.0):**
  - 최신 웹소설 서브장르 키워드 50여 개(예: `1라운더`, `성골`, `살아남기`, `사이버펑크`, `홈마`, `달동네`, `자연과학원`, `맹인궁수`, `산적왕`, `음악천재`, `메이지`, `다크메이지`, `말세천재`, `둔화`, `대항해`, `대해적`, `우지파`, `견야차`, `고무`, `신공` 등)를 사전 데이터베이스에 반영.

### Fixed

- **문장부호 회차 경계 정규식 정교화 (`core/title_anchor_extractor.py`):**
  - 전각 문장부호(`？`, `！`) 및 마침표(`.`) 바로 뒤에 공백 없이 회차 범위(`1-238`, `1-466`)가 연결된 경우, 회차 정보를 분리하지 못하고 제목에 잔존하던 엣지 케이스 정규식 수정을 통해 **순수 제목 추출 성공률 100%** 달성.
- **`.env` 네이버 API Key Base64 자동 복호화 (`modules/classifier/api_config_manager.py`):**
  - `.env` 파일에 Base64 인코딩 상태로 저장된 `NAVER_CLIENT_ID` 및 `NAVER_CLIENT_SECRET`을 자동으로 복호화하여 API 호출 시 **HTTP 401 Unauthorized** 오류 해결.
- **무작위 100개 샘플 검증 자동 분류 성공률 100% 달성:**
  - `list.txt`에서 신규 추출한 100개 무작위 샘플 검증 시 미분류 0건 (자동 분류 성공률 100%) 및 기존 코어 유닛 테스트 92개 100% 통과 유지.

## [v1.3.30] - 2026-08-01

### Added

- **웹소설 세부 특징 키워드 추출 모듈 신설 (`core/utils/novel_trait_extractor.py`):**
  - 메인 장르와 함께 웹 조사 스니펫, 플랫폼 태그, 제목 및 원본 태그로부터 핵심 특징 키워드(예: `시스템`, `궁정`, `궁투`, `빙의`, `책빙의`, `공간`, `농촌`, `가족`, `연대물`, `재테크`, `말세`, `종말`, `이세계`, `군사`, `첩보`, `로맨스`, `코미디`, `힐링`, `일상`, `드래곤볼` 등)를 추출하여 `[메인장르, 특징1, 특징2]` 태그(최대 3개 항목)로 정규화하는 유틸리티 신설.

### Fixed

- **다중 키워드 태그 및 언더바 범위 파싱 지원 (`core/title_anchor_extractor.py`):**
  - `[SF, 시스템]`, `[언정, 궁투, 빙의]` 등 다중 키워드가 결합된 기존 파일명 태그 감지 및 원본 제목 분리 정규식을 확장했습니다.
  - `_1_222 완` 또는 `_1_222` 형태의 언더바(`_`) 구분 범위 표기를 감지하여 `1-222 (완)`으로 정상 변환하고, 제목 끝의 연재/련재 상태 노이즈(`련재`/`연재`/`연재중`)를 정돈하도록 파싱 로직을 강화했습니다.
- **PyInstaller EXE 패키징 파이프라인 연동 (`build_exe.py`):**
  - `NovelTraitExtractor` 신규 모듈을 `hidden-import` 목록에 추가하고, 윈도우 환경 stdout/stderr UTF-8 인코딩 처리 및 `WNAP_Manager_v1.3.30.exe` 실행 파일 패키징을 자동화했습니다.

## [v1.3.29] - 2026-03-08

### Fixed

- **파이프라인 파일명 정규화 엔진(`core/title_anchor_extractor.py`) 오류 전면 수정:**
  - **한글 자모(이중 모음) 오접합 해결:** 시각적 변형 복원(야민정음) 로직 파이프라인 적용부에서 이중 모음 치환 테이블(`ㅘ, ㅝ, ㅚ...`)이 완전하지 않아 `귀호ㅏㄴ` 등으로 잘못 조합되던 문제를 수정했습니다.
  - **다중 부가 정보 잘림 현상 해결:** `+ 에필, 후기,` 패턴과 같이 쉼표로 나열된 여러 외전/후기 텍스트가 정규식(콤마/한글 범위 미흡) 한계로 인해 잘리거나 무시되던 현상을 해결하고, 마지막 남는 쉼표를 정확히 제거하도록 수정했습니다.
  - **숫자 0 보정 / 복합 패턴 인식 추가:** `에필로그 N-M` 등의 복합 패턴이 화수 번호에 묻히지 않도록 사전 분리 규칙을 강화하고, `00150` 등의 단독 범위에서 선행 0을 깔끔하게 제거합니다.
  - **'번외포함' 등 신규 키워드 인지:** `번외포함`, `N장` 등을 단위로 처리할 수 있도록 식별자를 추가했습니다.
  - **GUI / 파이프라인 동기화:** `rename_normalize.py`뿐 아니라 `main.py` 구동 시 활용되는 파이프라인 코어 로직의 정규화 일관성을 100% 확보했습니다.

## [v1.3.24] - 2026-02-24

### Fixed

- **파일명 정규화 시 제목 잘림 현상 수정 (`core/title_anchor_extractor.py`):**
  - "바보 성녀님은 2회차가 낯설어 1-371 完.txt"와 같이 제목에 포함된 "2회차가" 부분이 단일 숫자(권/회 등) 파싱 패턴에 잘못 매칭되어 삭제되는 현상을 수정했습니다.
  - 이제 숫자 뒤에 "회", "권" 등의 단위가 오더라도 뒤에 다른 일반 문자("차가" 등)가 바로 이어지는 경우(완결 마커/공백 제외)에는 식별자(화/권/부 등)로 간주하지 않고 제목의 일부로 보존합니다.

## [v1.3.23] - 2026-02-24

### Fixed

- **GUI 정규화 미리보기 판본 누락 현상 수정:** `core/title_anchor_extractor.py`에 보존해둔 판본 정보(`[개정판]` 등)가 GUI의 `adapters/filename_normalizer_adapter.py` 및 `NovelTask` 객체를 거치면서 중간에 유실되던 구조적 버그를 수정하여 UI에 정확히 반영되도록 조치했습니다.

## [v1.3.22] - 2026-02-24

### Fixed

- **GUI 파일명 정규화 엣지 케이스 수정 (`core/title_anchor_extractor.py`):**
  - **`[현대 판타지 AI번역]` 등 복합 태그:** 태그 전체가 제거될 뿐만 아니라, 텍스트 내의 장르 토큰(`현대 판타지` 등)을 인식하여 정규화된 범용 장르(`현판` 등)로 추론하도록 개선했습니다.
  - **`[개정판]` 등 판본 정보 태그 보존:** 판본 정보가 장르로 추출되지 않도록 수정한 상태에서, 파일명에서 완전히 삭제되지 않고 제목 바로 뒤에 `[개정판]`과 같이 정보가 보존되도록 `TitleParseResult.edition_info` 필드를 신설했습니다.

## [v1.3.21] - 2026-02-24

### Fixed

- **GUI 정규화 버그 수정 (`core/title_anchor_extractor.py`):**
  - **`[개정판]`이 장르로 오추출되던 버그 수정:** `GENRE_TAG_PATTERNS`에서 `개정판`, `합본`, `특별판`을 제거하고 별도 `EDITION_TAG_PATTERNS`로 분리했습니다. 이제 판본 태그는 단순 제거되며 장르 컬럼에 나타나지 않습니다.
  - **`[현대 판타지 AI번역]` 복합 태그 미처리 버그 수정:** `PLATFORM_TAG_PATTERNS`에 `[장르 + 번역]` 복합 태그 패턴을 추가하여 정상 제거됩니다.

### Refactored

- **`modules/organizer/renameFiles/rename_normalize.py` 주석 교정:** 잘못 붙어 있던 "GUI 정본 파일" 주석을 제거하고, 실제 역할인 "구버전 독립형 도구(Legacy Standalone)"로 교정했습니다.
  - 실제 GUI 정규화 엔진: `core/title_anchor_extractor.py` + `core/adapters/filename_normalizer_adapter.py`

## [v1.3.20] - 2026-02-24

### Changed

- **진입점(엔트리포인트) 일원화:**
  - 기존 `main.py`(CLI 전용)와 `main_gui.py`(GUI 전용)를 `main.py` 단일 파일로 통합했습니다.
  - 인자 없이 실행하거나 `--gui` 플래그를 사용하면 GUI 모드, `-s <폴더>`를 지정하면 CLI 파이프라인 모드로 동작합니다.
  - `build_exe.py` 빌드 진입점도 `main_gui.py` → `main.py`로 변경했습니다.

### Refactored

- **모듈 구조 정리 (중복 파일 제거):**
  - `modules/normalizer/rename_normalize.py`가 GUI에 사용되지 않는 고아 파일임을 확인하고 `_cleanup_temp`로 이동했습니다. (정본: `modules/organizer/renameFiles/rename_normalize.py`)
  - `modules/normalizer/rename_normalize_gui.py`, `build_exe.py`, `VERSION.txt`, `requirements.txt` 이동.
  - `modules/organizer/renameFiles/` 내 구버전 빌드 스크립트(`build.bat`, `build.ps1`, `create_release.py`, `FileNameNormalizer.spec`) 이동.
  - 정본 파일(`modules/organizer/renameFiles/rename_normalize.py`) 상단에 "이 파일이 GUI 정본" 주석 추가로 재발 방지.
- **버전 관리 중앙화:** `core/version.py`가 모든 모듈의 유일한 버전 소스임을 확립했습니다.

## [v1.3.19] - 2026-02-24

### Fixed

- **파일명 정규화 버그 수정:**
  - **`[현대 판타지 Aㅣ번역]` → `[현판]` 변환 처리:** `detect_category`의 동의어 매핑에 번역 노이즈 제거 로직을 추가하여, `Aㅣ번역`(야민정음) · `AI번역` · `기계번역` 등을 제거한 뒤 장르를 식별합니다.
  - **`[개정판]1-202`의 공백 누락 수정:** `remove_author_info`에서 보존 태그(`[개정판]` 등) 복원 시 앞뒤에 공백을 자동으로 삽입합니다.
  - **`1-202 완결` 패턴에서 `완결` 삭제 오류 수정:** 범위 뒤 작가명 제거 패턴(`remove_author_info`)의 보호 목록에 `완결`·`완`·`完` 키워드가 누락되어 작가명으로 오인되던 버그를 수정했습니다.
- **장르 오추론 버그 수정:**
  - **`[개정판]` 텍스트가 장르로 추론되는 오류 수정:** `normalize_line`에서 장르 추론 전(前) `[개정판]`·`[합본]` 등 비(非)장르 대괄호 태그를 임시 제거한 후 키워드 기반 추론을 수행합니다.

## [v1.3.18] - 2026-02-21

### Added

- **소스 폴더 즉시 적용 기능 (GUI):**
  - `[소스 폴더에 저장]` 버튼을 새로 추가하여, 2단계 파싱 완료 후 장르 추론 단계 없이 원본 소스 파일의 이름을 즉시 정규화된 이름으로 교체(`os.rename`)할 수 있는 기능을 제공합니다.
  - `일괄 처리` 모드 진행 시에도 이 즉시 적용 단계를 매끄럽게 거쳐가도록 파이프라인 흐름을 보강했습니다.

### Fixed

- **한글 자소 분리 및 시각적 변형(야민정음) 복원:**
  - NFD 분리뿐 아니라 필터 우회 목적으로 작성된 변형(`ㄷH공ㅂlㄱr`, `ㄹㅔㅂ 1` 등) 텍스트를 파이프라인 내에서 자동으로 원래 한글(`대공비가`, `렙 1`)로 조립해주는 커스텀 로직 `compose_korean_jamo`를 추가했습니다.
  - 영문자 기반 파일에서는 오작동하지 않게 예외 조건을 강화했습니다.
- **파일명 내 단일 숫자 추출 오류 해결:**
  - `렙 1 이지만`과 같이 단위가 없고 문자가 바로 뒤따르는 숫자를 불필요하게 '범위(연재 화수)'로 오인하여 추출하는 정규식 버그를 해결했습니다.

## [v1.3.16] - 2026-02-03

### Fixed

- **구글 검색 장르 추론 개선:**
  - **심층 스크래핑 로직 강화:** 검색 스니펫에서 장르 정보가 부족하거나 너무 일반적인 경우(예: '소설'), **정보성 사이트(디시인사이드, 나무위키, 아카라이브 등)를 자동으로 스크래핑**하여 본문 내용을 분석하도록 개선했습니다.
  - **장르 우선순위 적용:** 본문에서 여러 장르가 발견될 경우, 더 구체적이고 특징적인 장르를 우선 선택합니다.
    - 예: `현대인 선협세계 빙의물` -> '무협/판타지' 대신 **[선협]** 채택
    - 예: `울트라맨 팬픽...` -> '판타지' 대신 **[패러디]** 채택

## [v1.3.15] - 2026-02-03

### Fixed

- **Hotfix:** 보존된 장르 태그가 '장르 추론(Inference)' 단계에서 검색 결과로 덮어씌워지는 문제를 수정했습니다. (장르가 이미 확정된 경우 API 검색 건너뜀)

## [v1.3.14] - 2026-02-03

### Fixed

- **Hotfix:** 장르 태그 보존 로직이 실제 파일명 생성 시 적용되지 않던 문제를 수정했습니다. (정규화 어댑터 로직 보완)

## [v1.3.13] - 2026-02-03

### Fixed

- **장르 분류 로직 개선:**
  - **소설넷 오분류 수정:** '로맨스, 판타지, 현대판타지' 등의 태그가 혼재된 경우, **'현대판타지'** (구체적 장르)가 '로판'보다 우선순위를 갖도록 수정했습니다.
  - **구글 검색 '패러디' 지원:** 검색 결과(스니펫/본문)에서 `팬픽`, `패러디`, `2차 창작` 키워드 발견 시 **[패러디]** 장르로 정확히 분류합니다. (예: "미국만화 팬픽" 등)
  - **장르 태그 보존:** 파일명에 이미 존재하는 `[선협]` 등의 장르 태그를 노이즈로 삭제하지 않고 보존하여 정규화에 반영합니다.

## [v1.3.12] - 2026-02-03

### Added

- **장르 수동 편집 기능:**
  - 결과 테이블에서 **장르(Genre)** 컬럼을 더블클릭하여 직접 장르를 수정할 수 있습니다.
  - 장르 수정 시, 파일명 앞의 `[장르]` 태그도 자동으로 동기화되어 업데이트됩니다.

## [v1.3.11] - 2026-02-03

### Fixed

- **제목 잘림(Truncation) 오류 수정:**
  - `면역자`, `원자력` 등 일반 단어 내부에 `역자`, `자력` 등의 키워드가 포함될 경우, 이를 메타데이터 태그로 오인하여 제목 뒷부분을 잘라버리는 문제를 수정했습니다.
  - 이제 키워드 감지 시 단어의 경계(Boundary)를 엄격하게 확인합니다.

## [v1.3.10] - 2026-02-03

### Changed

- **복합 파일명 정규화 지원 강화:**
  - `1부 완결 2부` 등이 섞여 있는 복잡한 파일명 패턴(`1-546 完 2부 212`)을 감지하여, 순서를 억지로 바꾸지 않고 원본 흐름을 보존(`1-546 (완) 2부 1-212`)하도록 개선했습니다.
  - 개선된 로직은 복합 패턴 이후에 나오는 `외전`, `후기` 등의 부가 정보도 놓치지 않고 정확히 파싱합니다.

## [v1.3.9] - 2026-02-03

### Changed

- **폴더 정리 기능 개선:**
  - 폴더 평탄화(Flatten) 로직을 강화하여 서브폴더와 압축 파일 내부의 파일들을 모두 최상위(Root) 폴더로 바로 꺼내도록 수정했습니다. `[26.02]...` 같은 불필요한 서브폴더가 생성되지 않습니다.
  - 폴더 정리 후 파일 목록을 다시 스캔할 때, 백업 폴더(`Temp`)가 포함되지 않도록 제외 처리하여 리스트가 정확해졌습니다.
  - "1. 폴더 정리" 버튼이 Dry-Run(미리보기) 전용에서 실제 실행(Action) 모드로 변경되었습니다.
- **파일명 정규화 로직 개선:**
  - 숫자나 단위 뒤에 공백 없이 붙은 완결 마커(`1부完`, `5권완`)가 정상적으로 인식되도록 정규식 패턴을 수정했습니다. (결과: `1부 (완)`)
- **일괄 처리(Batch) 개선:**
  - "일괄 처리" 버튼 실행 시에도 수정된 폴더 평탄화 및 재스캔 로직이 정확히 적용되도록 프로세스를 통합했습니다.

### Added

- **CSV 저장 기능:** 기존 "CSV 열기" 버튼을 "CSV 저장"으로 변경하여, 현재 작업 목록을 CSV 파일로 내보낼 수 있습니다.
- **폴더 열기 기능 복구:** "폴더 열기" 버튼이 스마트 폴백(Smart Fallback)을 통해 가장 적절한 폴더(Target > Source > Default)를 열도록 수정했습니다.

## [1.3.8] - 2026-02-02

### ⚡ 개선 사항

- **폴더 정리 기능 (Folder Organizer)**:
  - **평탄화 및 백업 (Flatten & Backup)**: '폴더 정리' 실행 시 하위 폴더의 파일들을 최상위 폴더(Root)로 이동시키고, 원본 폴더 구조는 `Temp` 폴더로 안전하게 백업합니다.
  - **연계성 강화**: 폴더 정리 후 변경된 파일 위치를 자동으로 인식하여 다음 단계(장르 분석, 정규화)와의 파이프라인 연계를 보장합니다.
- **정규화 로직 (Normalization Logic)**:
  - **복합 완결 패턴 개선**: `完+ 외전` 같은 복합 패턴을 `(완) + 외전`으로 정확히 분리합니다.
  - **숫자+단위 파싱 개선**: `1920 2부`와 같이 숫자 뒤에 단위(부, 권, 화)가 붙을 때 숫자가 제목에 남는 오류를 수정했습니다.
  - **추가 외전/노이즈 태그**: `외포`(외전 포함) 키워드 인식 및 `(AI 번역)` 노이즈 제거 패턴을 보강했습니다.

## [1.3.6] - 2026-01-09

### ⚡ 개선 사항

- **정규화 로직 (Normalization Logic)**:
  - **새로운 외전 키워드**: '후일담' 및 '특외'를 외전 식별자로 추가 지원합니다.
    - 예시: `... 完 후일담.txt` → `... (완) + 후일담.txt`

## [1.3.5] - 2026-01-02

### ⚡ 개선 사항

- **정규화 로직 (Normalization Logic)**:
  - **(19N) 태그 보존**: 파일명에서 `(19N)` 태그가 더 이상 제거되지 않습니다.
  - **범위 정규화**: `001-242` 스타일의 범위를 `1-242`로 정규화합니다 (선행 0 제거).
  - **미분류 장르 처리**: 장르를 알 수 없는 경우 `[미분류]` 태그를 생략합니다 (예: `[미분류] Title.txt` 대신 `Title.txt`).

## [1.3.4] - 2026-01-02

### 🛠 수정 사항

- **외전 파싱 (Side Story Parsing)**:
  - `TitleAnchorExtractor`의 '외전' 및 '에필로그' 추출 로직 개선
  - 'Epilogue'가 'Side Story'를 가리는 문제 해결 (루프 추출 구현)
  - 감지 방해 요소인 연결어('및', '포함') 제거
- **GUI 안정성 (GUI Reliability)**:
  - 독립형 정규화 GUI에서 치명적인 import 오류 수정
  - `rename_normalize_gui.py`에서 상대 경로 import를 위한 `sys.path` 패치 적용
  - 오류를 숨기던 조용한 fallback 제거

## [1.2.0] - 2024-12-28 (Retroactive)

- 최근 개선 사항을 위한 기본 버전

## [1.0.0] - 2024-12-28

### 🎉 최초 릴리스

#### 추가 사항

- 3개 모듈 통합 파이프라인 완성
  - FolderOrganizer: 압축 해제 및 폴더 정리
  - GenreClassifier: 키워드 기반 장르 분류
  - FilenameNormalizer: 표준 파일명 생성
- Title Anchor Parsing 알고리즘 구현
  - 노이즈 제거 (저자명, 번역 정보, 플랫폼 태그)
  - 핵심 제목 추출 및 잔여 문자열 파싱
- 중국 소설 특수 처리
  - 선협(仙俠) 키워드 인식
  - 언정(言情) 키워드 인식
  - ~지(志), ~기(記) 패턴 감지
- GUI 대시보드 (CustomTkinter)
  - 폴더 선택 및 설정 패널
  - 실시간 진행 상황 표시
  - 장르 확인 다이얼로그 (Medium confidence)
  - 결과 테이블 및 로그 뷰어
- CLI 인터페이스
  - argparse 기반 옵션 파싱
  - --source, --target, --dry-run, --yes 옵션
- Dry-run 모드
  - 파일 시스템 변경 없이 미리보기
  - mapping_preview.csv 생성
- Fault Isolation (결함 격리)
  - 개별 파일 오류 시 해당 파일만 skip
  - 재시도 메커니즘 (max_retries 설정)
- 138개 테스트 통과
  - Unit Tests: 100+
  - Property-based Tests: 16 (Hypothesis)
  - Integration Tests: 10+
- PyInstaller EXE 패키징
  - 단일 실행 파일 생성
  - build_exe.py 빌드 스크립트

#### 아키텍처

- Adapter Pattern: 기존 모듈 무수정 래핑
- NovelTask 중심 설계: 통합 데이터 모델
- Pipeline Pattern: Stage 1 → 2 → 3 순서 보장
- Spec-Driven Development: 요구사항 → 설계 → 구현 → 테스트

---

## [Unreleased]

### 예정 사항

- 다중 플랫폼 지원 (macOS, Linux)
- 장르 분류 AI 모델 통합
- 배치 처리 성능 최적화
- 플러그인 시스템
