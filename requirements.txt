# WNAP - Web Novel Archive Pipeline
# Python 3.10+ required

# GUI (CustomTkinter & Image handling)
customtkinter>=5.2.0
Pillow>=10.0.0

# Archive handling (Requires 7z.exe, UnRAR.exe for extraction)
py7zr>=0.20.0
rarfile>=4.0

# Web scraping & API (Naver Search, Google CSE, Platform Extractors)
requests>=2.28.0
beautifulsoup4>=4.11.0
selenium>=4.8.0

# Security & Configuration (.env & API Key Fernet encryption)
cryptography>=40.0.0
python-dotenv>=1.0.0

# Testing (Unit testing & Property-based testing)
pytest>=7.0.0
hypothesis>=6.0.0

# Packaging (Optional - build_exe.py)
pyinstaller>=6.0.0
